# Dependencies for the daily snapshot refresh job (src/refresh.py).
# Kept separate from requirements.txt so the deployed app stays lean — it only
# ever reads the prebuilt Parquet snapshot and never pulls live data.
pandas>=2.2
numpy>=1.26
pyarrow>=16
requests>=2.31
pybaseball>=2.2
